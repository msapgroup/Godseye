# GODSEYE v4.4 — Integrations Analytics Polish

- Removes the Prometheus Metrics link from the Integrations screen so users are no longer sent to the raw/broken metrics endpoint from the UI.
- Keeps the `/metrics` backend endpoint intact for authenticated/API-key Prometheus use.
- Changes integration analytics metric values and supporting analytics text to the same blue family used by the Pi-hole integration badge.
- Adds a brighter matching blue for analytics values in dark mode.
