# GODSEYE v4.5 — Integration Card Pi-hole Blue

- Changes integration-card supporting metadata to the Pi-hole blue family.
- Target URL, last-sync timestamp, and enabled/interval/credential metadata now use the same blue family as Pi-hole analytics.
- Dark mode uses the brighter #9cc9f5 variant for readability.
- Integration names, labels, buttons, and success state remain visually distinct.
